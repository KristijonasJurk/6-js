const name1= 'vardenis';
const name2= 'vardeniauskas';

function sayhi (vardas) {
    return 
}
console.log(`labas as esu, ${(name1)}, ka tu?`);
console.log(`labas as esu, ${(name2)}, ka tu?`);

function sum(a, b, c) {
    console.log(a, b, c);
    return `susumavus ${a}, ${b}, ${c} gauname ${a + b+ c}.`
}
console.log(sum(1, 2, 3));
console.log(sum(11,22,33));
